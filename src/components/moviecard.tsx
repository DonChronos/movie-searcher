import React from 'react';
import styled from 'styled-components';
import { Link } from 'react-router-dom';

const Movie = (props: <any></any>) => {
	<Link to={``}>
	<div>
	<h3>Title</h3>
	</div>
	</Link>
}

export default Movie;